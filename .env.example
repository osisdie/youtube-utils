# Slack Bot Token (xoxb-...) — get from https://api.slack.com/apps
# Required for bidirectional Slack communication (send + receive replies)
SLACK_BOT_TOKEN=

# Slack Channel ID for #update-vibe (starts with C)
# Find it: curl -s -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
#   "https://slack.com/api/conversations.list?types=public_channel&limit=200" \
#   | jq '.channels[] | select(.name | contains("update-vibe")) | .id'
SLACK_CHANNEL_ID=

# (Optional) Webhook URL fallback — used if no Bot Token
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ


# Whishper from OpenAI API
OPENAI_API_KEY=

# Whishper from HugginfFace inference API
HF_TOKEN=
